server:
  port: 8008

spring:
  application:
    name: config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/lijtforgithub/study-spring-cloud/cloud-config
          searchPaths: '/global/,/{application}/'
          username: lijtforgithub
          password: githubofljt0828
          

      
