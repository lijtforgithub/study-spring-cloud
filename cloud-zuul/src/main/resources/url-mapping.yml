pp:
  file-name: url-mapping

  redirect-list:
    - origin: /api/consumer/xxoo1
      redirect: /self/port
    - origin: /api/consumer/xxoo2
      redirect: /api/port
      serviceId: service-provider

  black-list:
    - 192.168.2.29
  white-list:
    - /api/consumer/white
  free-list:
    - /api/consumer/free
